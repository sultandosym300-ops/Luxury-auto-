# Optional: set to your Railway backend URL to call the API directly
# without going through Vercel rewrites.
# If not set, all /api/* requests go through vercel.json rewrites.
#
# Example:
# VITE_API_URL=https://workspaceapi-server-production-0d86.up.railway.app
VITE_API_URL=
